# GitHub Actions repository secrets (Settings → Secrets and variables → Actions)
# Generate local file: npm run env:init:secrets
# Overwrite existing: npm run env:init:secrets:force
# Then copy from .secrets into GitHub — do not commit .secrets
#
# Demo values match ecommerce-store-api docs/development/SEEDING.md (local dev only).

# Required — Playwright e2e job fails if admin email/password are unset
E2E_ADMIN_EMAIL=admin@store.local
E2E_ADMIN_PASSWORD=Admin123!

# Operator gate + change-password specs
E2E_CUSTOMER_EMAIL=customer@store.local
E2E_CUSTOMER_PASSWORD=Customer123!
E2E_SUPERADMIN_EMAIL=superadmin@store.local
E2E_SUPERADMIN_PASSWORD=SuperAdmin123!
